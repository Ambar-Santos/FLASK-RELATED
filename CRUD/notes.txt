1. Data Structure Set up 

Employee Management App 

= Model for Employee
  - id 
  - employee_id, 
  - name,
  - age,
  - position 

Install Flask SQLAlchemy 


2. Main application set up

- import flask 
- database configuration 
- Create the tables before the app can use them 

3. Creting records

- Create route that handles 
   - showing the form (GET) 
   - Processing the submited data (POST)

   db.session.add() ---> Stages new record
   db.session.commit() ---> Saves it to the database



   Notes: url RULE 